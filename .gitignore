# --- Planning / process files (NOT part of the submission; never commit) ---
_planning/
round2details.md
docs/
.superpowers/

# --- Data (provided datasets; large, do NOT commit) ---
# The police CSV is ~109 MB and exceeds GitHub's 100 MB file limit.
jan to may police violation_anonymized*.csv
Astram event data_anonymized*.csv
*.csv.gz

# --- Generated artifacts ---
# Dashboard/API are read-only consumers (no build step on Streamlit Cloud),
# so the 9 files app_data.py reads must ship in the repo. Everything else
# generated under artifacts/ (load-test logs, scratch parquet) stays ignored.
artifacts/*
*.parquet
!artifacts/scored_zones.parquet
!artifacts/hotspots.parquet
!artifacts/forecast.parquet
!artifacts/forecast_metrics.json
!artifacts/deploy_plan.parquet
!artifacts/roi_curve.parquet
!artifacts/blind_spots.parquet
!artifacts/validation.json
!artifacts/cis_sensitivity.json
briefings/

# --- Python ---
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/
.venv/
venv/
*.egg-info/

# --- OS / editor ---
.DS_Store
.vscode/
.idea/

# --- Secrets ---
.env
*.key
.gstack/

hero.html